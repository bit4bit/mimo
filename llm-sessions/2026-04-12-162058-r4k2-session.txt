[2026-04-12 16:10:01] User: in session page, when i press C-m i want to focus the user input box or where is showing 'Type a message...'
[2026-04-12 16:11:10] LLM: Got it — I traced this in the session chat frontend and here’s the implementation plan. - The input is the contenteditable bubble in `packages/mimo-platform/public/js/chat.js:176`, with placeholder `Type a message...`. - Right now there is only a local shortcut (`Ctrl+Enter`) inside that bubble at `packages/mimo-platform/public/js/chat.js:912`; there is no page-level `C-m` handler yet. Plan: - Add a small focus helper in `packages/mimo-platform/public/js/chat.js` that: - finds the editable bubble’s `.message-content`, - focuses it and places cursor at end, - creates the bubble first only if it’s missing and chat is not currently streaming. - Add a document-level `keydown` listener in `setupEventListeners()` (`packages/mimo-platform/public/js/chat.js:1480`) for `Ctrl+M` (`e.ctrlKey && e.key.toLowerCase() === 'm'`) that calls that helper. - Guard against hijacking other fields/modals by ignoring the shortcut when focus is already in a non-chat input/textarea/select/button/contenteditable element. - Add a failing-first test, then implement: - new test file under `packages/mimo-platform/test/` to assert pressing `Ctrl+M` focuses chat input behavior on session page wiring. - Run `bun test` for mimo-platform to verify no regressions. If you want, next step I’ll execute this plan and patch it.
[2026-04-12 16:11:22] User: do it
[2026-04-12 16:20:30] LLM: Implemented — `Ctrl+M` now focuses the chat input (“Type a message...”) on the session page. - Added a global keyboard shortcut handler in `packages/mimo-platform/public/js/chat.js:1515`. - Added `focusEditableBubbleInput()` in `packages/mimo-platform/public/js/chat.js:1475` to focus the editable bubble and place cursor at the end. - Added `shouldIgnoreFocusShortcutTarget()` in `packages/mimo-platform/public/js/chat.js:1499` so the shortcut does not hijack focus from other form fields/buttons/contenteditable areas. - The shortcut also restores the editable bubble first (when possible) if it is missing and the chat is not currently streaming. BDD-first test added: - New failing-first test: `packages/mimo-platform/test/chat-shortcut-focus.test.ts:51` - It loads the real `public/js/chat.js`, simulates `DOMContentLoaded`, fires `Ctrl+M`, and verifies the editable chat input is focused and default is prevented. Validation run: - ✅ `bun test test/chat-shortcut-focus.test.ts` passes. - ⚠️ Running with an existing recovery test showed one unrelated pre-existing failure in `packages/mimo-platform/test/chat-input-recovery.test.ts:115` (not touched by this change). If you want, I can also add a tiny UI hint near the input header like `⌃M Focus` to make the shortcut discoverable.
[2026-04-12 16:20:45] User: good commit and  push
