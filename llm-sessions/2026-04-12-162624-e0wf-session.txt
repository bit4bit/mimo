[2026-04-12 16:22:01] User: in page session the commit message is not in commit message on the upstream repository
[2026-04-12 16:23:10] LLM: You’re right — I traced the flow and found the mismatch. - The session page sends your text in `packages/mimo-platform/public/js/commit.js:46` as `{ message }`. - The backend route explicitly ignores it in `packages/mimo-platform/src/commits/routes.ts:18`. - The commit layer also hardcodes a timestamp message in `packages/mimo-platform/src/vcs/index.ts:770`. - So upstream always gets `Mimo commit at <timestamp>`, not what you typed. Plan to fix (BDD-first, no edits yet in this mode): - Add a failing integration test proving user-provided message appears in upstream commit subject (service-level, real git repo) in `packages/mimo-platform/test/commits.test.ts`. - Add/adjust a failing test around `commitUpstream` so explicit message is used when provided, and default timestamp is only fallback in `packages/mimo-platform/test/vcs.test.ts`. - Implement message propagation end-to-end: - Parse JSON body in `packages/mimo-platform/src/commits/routes.ts` and pass `message`. - Update `commitAndPush(sessionId, message?)` in `packages/mimo-platform/src/commits/service.ts`. - Update `commitUpstream(upstreamPath, repoType, message?)` in `packages/mimo-platform/src/vcs/index.ts`. - Keep backward compatibility: if message is missing/blank, fall back to timestamp auto-message. - Run focused tests for commits/vcs flow and report results. If you want, I can execute this plan now in implementation mode.
[2026-04-12 16:23:20] User: go
[2026-04-12 16:26:05] LLM: Fixed — the session commit message now flows through to the upstream commit instead of being replaced by the timestamp title. - I traced the path and found the break: UI sent `message`, but backend ignored it and `vcs.commitUpstream` always forced `Mimo commit at ...`. - I added failing tests first, then implemented the fix end-to-end. **What changed** - `packages/mimo-platform/src/commits/routes.ts`: reads optional JSON body `message` and passes it into commit service for both `/commit-and-push` and alias route. - `packages/mimo-platform/src/commits/service.ts`: `commitAndPush` now accepts `commitMessage?: string` and forwards it to VCS. - `packages/mimo-platform/src/vcs/index.ts`: `commitUpstream` now accepts `message?: string`; uses trimmed custom message when provided, otherwise keeps existing timestamp fallback behavior. **BDD tests added** - `packages/mimo-platform/test/commits.test.ts`: new integration test verifies `commitService.commitAndPush(sessionId, userMessage)` produces `git log -1 --pretty=%s === userMessage`. - `packages/mimo-platform/test/vcs.test.ts`: new test verifies `commitUpstream(..., "git", customMessage)` uses the explicit message. **Validation** - `bun test test/commits.test.ts` → pass. - Focused new tests: - `bun test test/commits.test.ts -t "user-provided commit message"` → pass. - `bun test test/vcs.test.ts -t "explicit commit message"` → pass. - Full `test/vcs.test.ts` still has an unrelated pre-existing Fossil test failure (`should commit in Fossil upstream with timestamp message`), not introduced by this fix. If you want, I can also add a route-level integration test for `POST /commits/:sessionId/commit-and-push` to lock this behavior at API boundary.
[2026-04-12 16:26:12] User: good, commit and push
