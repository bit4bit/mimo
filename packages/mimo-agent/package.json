{
  "name": "mimo-agent",
  "version": "0.1.0",
  "description": "mimo-platform agent - proxies ACP protocol and watches file changes",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile dist/mimo-agent",
    "test": "bun test",
    "test.full": "bun test --config /dev/null test/ integration-test/"
  },
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "0.25.3",
    "@agentclientprotocol/sdk": "^0.18.0",
    "jose": "^6.2.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "bun-types": "latest",
    "prettier": "^3.8.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
